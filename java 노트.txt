클래스

클래스 tv 설계도, 객체 tv
객체 - 인스턴스

객체는 속성과 기능으로 이루어져있다.
속성 멤버변수 특성 필드 상태 (정적인것들)
기능 메서드 함수 행위 (동적인것들)

printf(서식) = 아직 안함.;

데이터 저장형태의 발전과정
변수 -> 배열(정수) -> 구조체 -> 클래스(함수 추가됨)

1. 캡슐화
2. 상속
3. 추상화
4. 다형성

public : 어디서나 접근 가능
protected : 상속에서만 접근 가능
default : 같은 패키지 내에서만 접근 가능 - 아무것도 안적혀있으면 기본으로 디폴트
private : 외부에서는 접근 X 내부 클래스에서만 사용가능

멤버변수에는 private 을 붙인다.
메소드에는 public을 붙이는게 일반적

static void func(){} : static 키워드가 붙어있는 메소드는 객체 생성하지 않고 접근 가능

void func(){} : static 없는 메소드는 반드시 객체 생성하고 접근

static 유무 차이점.
유 (static 있는것.) = 





클래스 - 목적
변수 , 메소드
변수 - 클래스 변수(정적변수), 인스턴스 변수, 지역변수
메소드 - 클래스메소드, 인스턴스메소드
--> 클래스는 지들끼리 논다.

- 생성자 - 변수 초기화 용도.
생성자 오버로딩?

this의 2가지 기능
1. 객체 자기 자신을 참조
2. 다른 생성자 호출

getter, setter
- 변수가 private로 외부에서 접근할 수 없기 때문에 외부 클래스에서 접근 통로를 만들어 주는 것.

인스턴스 변수를 초기화 또는 변경하는 방법은 크게 2가지이다.
- 생성자, setter

클래스는 반드시 객체 생성 후 사용!
static 변수 및 static 메소드는 	클래스명.static변수, 	클래스명.static메소드	로 호출 가능
단, 같은 클래스 내에서는 클래스 명은 생략하고 static변수, static메소드 사용가능.

